export class Image {
  constructor({ aspect_ratio, file_path, width, height }) {
    this.aspect_ratio = aspect_ratio;
    this.file_path = file_path;
    this.height = height;
    this.width = width;
  }
}
